allprojects {
    group 'org.qjson'
    version '0.9-SNAPSHOT'
    apply plugin: 'java'
    compileJava {
        options.compilerArgs << '-parameters'
    }
    sourceSets {
        test {
            resources {
                srcDirs = ['src/test/java']
            }
        }
    }
}
