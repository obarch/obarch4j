dependencies {
    implementation(
            project(':codec:codec'),
            project(':codec:core'),
            'org.jetbrains:annotations:16.0.2'
    )
    testImplementation(
            project(':livedoc'),
            'junit:junit:4.12',
    )
}

