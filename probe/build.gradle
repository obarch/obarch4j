allprojects {
    group 'com.obarch.probe'
    version '0.9-SNAPSHOT'

    apply plugin: 'java'
    tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }
    compileJava {
        options.compilerArgs << '-parameters'
    }
}
